apiVersion: v1
kind: Service
metadata:
  name: vault-transit
spec:
  selector:
    app: vault-transit
  ports:
    - name: http
      port: 8200
      targetPort: 8200
    - name: cluster
      port: 8201
      targetPort: 8201
  type: ClusterIP
